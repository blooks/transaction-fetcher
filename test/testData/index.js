var addresses = require('./addresses');
var wallet = require('./wallet');
var transfers = require('./transfers');

module.exports = {
  addresses: addresses,
  wallet: wallet,
  transfers: transfers
};
