machine:
  node:
    version: 6
  services:
    - docker

deployment:
  master:
    branch: master
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker build -t blooks/transaction-fetcher:master --build-arg NODE_ENV=production .
      - docker push blooks/transaction-fetcher:master
  versionbuild:
    tag: /v[0-9]+(\.[0-9]+)*/
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker build -t blooks/transaction-fetcher:$CIRCLE_TAG --build-arg NODE_ENV=production .
      - docker push blooks/transaction-fetcher:$CIRCLE_TAG
